networks:
  coolify:
    external: true

services:
  caddy:
    build: .
    environment:
      FIRECRAWL_API_KEY: ${FIRECRAWL_API_KEY}
      FIRECRAWL_UPSTREAM: "api:3002"
    expose:
      - "3000"
    networks:
      - default
      - coolify
    labels:
      - "traefik.http.routers.http-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy.service=http-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy"
      - "traefik.http.routers.https-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy.service=https-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy"
      - "traefik.http.services.http-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy.loadbalancer.server.port=3000"
      - "traefik.http.services.https-0-sk1yu9lctc2r4wi1xx7vvhu0-caddy.loadbalancer.server.port=3000"
    depends_on:
      api:
        condition: service_started

  api:
    image: ghcr.io/firecrawl/firecrawl:latest
    command: node dist/src/harness.js --start-docker
    restart: on-failure
    environment:
      HOST: "0.0.0.0"
      PORT: "3002"
      REDIS_URL: "redis://redis:6379"
      REDIS_RATE_LIMIT_URL: "redis://redis:6379"
      PLAYWRIGHT_MICROSERVICE_URL: "http://playwright:3000/scrape"
      POSTGRES_HOST: "nuq-postgres"
      POSTGRES_PORT: "5432"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-postgres}"
      POSTGRES_DB: "postgres"
      USE_DB_AUTHENTICATION: "false"
      NUM_WORKERS_PER_QUEUE: "${NUM_WORKERS_PER_QUEUE:-8}"
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
      BULL_AUTH_KEY: "${BULL_AUTH_KEY:-@}"
      NUQ_RABBITMQ_URL: "amqp://rabbitmq:5672"
    expose:
      - "3002"
    depends_on:
      redis:
        condition: service_started
      playwright:
        condition: service_started
      nuq-postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    ulimits:
      nofile:
        soft: 65535
        hard: 65535

  playwright:
    image: ghcr.io/firecrawl/playwright-service:latest
    environment:
      PORT: "3000"
    expose:
      - "3000"

  rabbitmq:
    image: rabbitmq:3-management
    expose:
      - "5672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 5s

  redis:
    image: redis:alpine
    command: redis-server --bind 0.0.0.0
    expose:
      - "6379"
    volumes:
      - redis_data:/data

  nuq-postgres:
    image: ghcr.io/firecrawl/nuq-postgres:latest
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-postgres}"
      POSTGRES_DB: "postgres"
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 10

volumes:
  redis_data:
  postgres_data:
